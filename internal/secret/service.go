package secret

// func CreateSecret(content string, duration int, views int, password string) (openapi.Secret, error) {
// 	// hash := sha256.Sum256([]byte(password))
// 	// hashPassword := fmt.Sprintf("%x", hash)

// 	secret := openapi.Secret{
// 		// ID:             uuid.NewString(),
// 		// Password:       hashPassword,
// 		// ExpiresAt:      time.Now().Local().Add(time.Second * time.Duration(duration)),
// 		// RemainingViews: views,
// 		// EncryptedText:  "Bob",
// 	}

// 	return secret, nil
// }
